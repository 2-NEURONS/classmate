# Retrieve instance connection name from the previous step
spring.cloud.gcp.sql.instance-connection-name=classmate-migration:us-central1:psql-neura-classmate-instance
spring.cloud.gcp.sql.database-name=users
# Cloud SQL starter automatically configures the JDBC URL
# Configure username/password
spring.datasource.username=postgres
spring.datasource.password=
# Configure connection pooling if needed
spring.datasource.hikari.maximum-pool-size=10



